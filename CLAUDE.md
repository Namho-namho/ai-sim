# AI 학습 시뮬레이션 사이트

상호작용 시뮬레이션으로 AI 개념을 배우는 웹사이트. PhET, TensorFlow Playground,
Explorable Explanations 계열을 참고한 구성.

## 대상 독자

초중등 학생 및 AI 비전공 성인. **수식을 쓰지 않는다.** 개념은 조작과 시각적 변화로 전달한다.
용어는 쉬운 말을 먼저 쓰고 정식 용어를 괄호로 병기한다. (예: "중요도(가중치)")

## 공통 뼈대 — 모든 모듈이 따른다

    데이터 → 모델 → 손실(또는 보상) → 학습

- **딥러닝 모듈**: `모델` 단계를 여러 층 쌓기로 확장. 나머지 단계 구조는 그대로.
- **강화학습 모듈**: `데이터`가 (상태, 행동, 보상)으로, `손실`이 보상 최대화로 치환.
- 각 모듈 마지막에는 "이 뼈대가 다른 분야에서 어떻게 반복되는지" 연결 문구를 넣는다.

이 뼈대를 깨는 구조를 제안하기 전에 반드시 사용자에게 먼저 확인할 것.

## 기술 스택 / 제약

- Vite + React 18. **서버 없음** (정적 빌드 후 배포).
- 상태 관리는 `useState` / `useRef` / `useEffect` 만 사용. 상태 관리 라이브러리 도입 금지.
- 스타일은 현재 **인라인 스타일** 방식. Tailwind, CSS-in-JS 라이브러리 도입은 사용자 승인 후.
- 라우팅은 `src/useHashRoute.js` 의 해시 라우팅. react-router 도입하지 않음 (정적 호스팅 호환).
- 새 npm 의존성 추가 시 반드시 이유를 설명하고 승인받을 것.
- `localStorage` / `sessionStorage` 사용 금지 (환경에 따라 동작하지 않음). 상태는 메모리에만 둔다.

## 폴더 구조

    src/
      main.jsx            진입점
      App.jsx             홈 목록 + 라우팅
      index.css           전역 스타일
      useHashRoute.js     해시 라우팅 훅
      modules/
        index.js          모듈 레지스트리 (새 모듈 등록 지점)
        MLBasicsModule.jsx

## 새 모듈 추가 절차

1. `src/modules/<이름>Module.jsx` 생성, default export 컴포넌트 작성
2. `src/modules/index.js` 에 import 후 `MODULES` 배열에 항목 추가
3. `status`를 `"planned"` → `"ready"` 로 변경
4. 홈 화면 목록과 라우팅은 레지스트리에서 자동 반영되므로 `App.jsx` 수정 불필요

## 시뮬레이션 설계 원칙

- **조작 → 숫자 변화 → 결과 변화**가 한 화면에서 동시에 보여야 한다. 스크롤로 분리하지 않는다.
- 값은 연속적으로 변해야 한다. 정답 개수처럼 뚝뚝 끊기는 지표보다, 손실처럼 미세한 조작에도
  반응하는 지표를 우선한다. (경사하강 직관을 위해)
- 시뮬레이션은 **실제 알고리즘**으로 동작시킨다. 미리 정해둔 애니메이션을 재생하지 않는다.
  현재 MLBasicsModule의 학습 규칙은 실제 퍼셉트론 갱신식이며, 정의된 손실의 경사하강과 일치한다.
- 사용자가 손으로 해보다 어려움을 느낀 **직후에** 자동화(학습)를 보여준다. 순서를 바꾸지 않는다.
- 예제 데이터셋에는 반드시 함정 사례를 포함한다. (특징 하나만으로는 풀리지 않도록)

## UI 원칙

- 장식 최소화. 애니메이션은 개념 전달에 기여할 때만.
- 모바일 700px 이하 대응, 키보드 포커스 표시, `prefers-reduced-motion` 존중.
- 한국어 UI. 문장은 짧게, 명령형 버튼 라벨 사용.

## 하지 말 것

- 모듈에 브랜딩성 이름 붙이기 (예: "고양이 탐정 학교"). 개념 이름을 제목으로 쓴다.
- 개념 설명 없이 화려한 시각화만 추가하기.
- 사용자 확인 없이 기존 모듈의 학습 알고리즘 변경하기.
